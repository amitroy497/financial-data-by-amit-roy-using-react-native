import { CommonViewUpdate } from '../../components';
import { AllNavigationName } from '../../constants';

export const ViewInvestments = () => {
	return <CommonViewUpdate location={AllNavigationName.viewInvestments} />;
};
